---
slug: /developers/sdk/packages/core
---

import DocCardList from "@theme/DocCardList";

# Core

The `@gardenfi/core` aims to abstract away the complexity in creating HTLC transactions. It provides a single `GardenJS` class that allows you to create orders, subscribe to them along with performing HTLC actions such as initiating, redeeming and refunding your orders.

<DocCardList />
